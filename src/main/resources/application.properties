# Database Configuration
spring.datasource.url=jdbc:postgresql://wavzedemodb.postgres.database.azure.com:5432/postgres
spring.datasource.username=jagadeesh
spring.datasource.password=Tarang2025!
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Configuration
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.default_schema=demo
spring.jpa.properties.hibernate.format_sql=true

# Server Configuration
server.port=8080

# CORS Configuration (will be handled in code)
spring.web.cors.allowed-origins=http://localhost:4200

